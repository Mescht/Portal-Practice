# stores the bottom most bit of "transforms" in "bit" and shitfts "transforms" to the right
scoreboard players operation bit vars = transforms vars
scoreboard players operation bit vars %= 2 c
scoreboard players operation transforms vars /= 2 c